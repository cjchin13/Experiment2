#include <iostream>
#include <conio.h>

using namespace std;

int main()
{
    int A, B;
    
    cout << "Enter a number: "; cin >> A;
    
    for(B = 1; B <= 10; B++)
    {
        cout << A << " * " << B << " = " << (A * B) << endl;
    }
    
    _getch();
    return 0;
}
